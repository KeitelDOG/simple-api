{
  "name": "simple-api",
  "version": "1.0.0",
  "description": "Simple API for testing other features like Nginx config, Docker config, JSON response etc.",
  "main": "index.js",
  "scripts": {
    "start": "node server",
    "dev": "nodemon server",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "Simple",
    "API"
  ],
  "author": "KeitelDOG",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "path": "^0.12.7"
  }
}
